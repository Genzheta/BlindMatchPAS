@model BlindMatchPAS.Web.Models.Student.ProjectProposalViewModel

@{
    ViewData["Title"] = "Submit Proposal";
}

<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card shadow-sm border-0">
            <div class="card-body p-4">
                <h2 class="mb-4">Submit a New Project Proposal</h2>
                <form asp-action="Create" method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    
                    <div class="mb-3">
                        <label asp-for="Title" class="form-label"></label>
                        <input asp-for="Title" class="form-control" placeholder="Enter a descriptive title" />
                        <span asp-validation-for="Title" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="ResearchAreaId" class="form-label"></label>
                        <select asp-for="ResearchAreaId" class="form-select" asp-items="@(new SelectList(Model.ResearchAreas, "Id", "Name"))">
                            <option value="">-- Select Research Area --</option>
                        </select>
                        <span asp-validation-for="ResearchAreaId" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Abstract" class="form-label"></label>
                        <textarea asp-for="Abstract" class="form-control" rows="5" placeholder="Provide a brief overview of your project idea..."></textarea>
                        <span asp-validation-for="Abstract" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="TechnicalStack" class="form-label"></label>
                        <input asp-for="TechnicalStack" class="form-control" placeholder="e.g. ASP.NET Core, SQL Server, Vue.js" />
                        <span asp-validation-for="TechnicalStack" class="text-danger"></span>
                    </div>

                    <div class="d-flex justify-content-between">
                        <a asp-action="Dashboard" class="btn btn-outline-secondary">Cancel</a>
                        <button type="submit" class="btn btn-primary">Submit Proposal</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
